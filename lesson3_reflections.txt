When would you want to use a remote repository rather than keeping all your work local?
A remote repository can be used as your project master for all contributors. The master file which always works and should therefore never fail

